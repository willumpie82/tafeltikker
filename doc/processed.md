# Processed requests

Completed items moved out of `new-functionality.md` to keep that file
focused on what's still open. Each entry keeps the original request plus
the note on how/why it was resolved.

## general
- [x] on the kids progress; currently only shows tafel 3 5/5 100%, I think it would be nice to get some more metrics on the actual progress and speed per excercise, hence kid can solve 1x3 no problem but 3x4 results more mistakes or takes significant longer, create suggestion how to display, e.g. bars with excersise confidence per 'tafel', since displaying all takes up lots of room make them able to collabse met [meer]-button, rollup the confidence score per 'tafel' when collapst — added elapsed_ms tracking to math_attempts (wasn't captured before), a per-fact confidence score combining accuracy + speed (formula and rationale in `parent-dashboard-metrics.md`), and a "meer ↓"/"minder ↑" toggle per table revealing 10 color-coded bars (one per multiplier) while the collapsed view keeps the existing rollup line
- [x] add a back & log-out button (exists on the home screen and inside the settings screens, but not once an exercise is running — a child can't get back to home or log out mid-session yet) — back arrows on both exercise views now end the session and return home, where logout is one tap away
- [x] design registration flow — invite-token-based registration: an admin generates a single-use, expiring link, the new parent opens /register.html?token=..., which validates the token before showing the signup form
- [x] invite new parent using invite url+key — /api/admin/invites (generate/list) + public /api/register/:token + /api/register; Invites tab in admin.html
- [x] system-admin can manage 'parents', reset password — Parents tab in admin.html, backed by /api/admin/parents
- [x] admin can promote 'parent' to user-admin — role dropdown in the Parents tab, system_admin only, capped at user_admin (system_admin itself only ever set via seed/DB)
- [x] userlevels: child, parent, user-admin (manage parents+childs, cannot see progress), system-admin same as user-admin + promote roles + create new users — parents.role column (parent/user_admin/system_admin), requireAdmin guard on all /api/admin/** routes, no stats exposed to admins by design
- [x] the screen is now highly optimized for mobile, make it more adaptive, sometimes with 'sommen' when the hint is displayed, parts drop of the screen. on widescreen show the hint left of the test — root cause was `align-items: center` on `<body>` silently clipping the top of overflowing content with no way to scroll to it; switched to `align-items: safe center`. On screens ≥900px the math exercise now shows the hint panel in a column to the left of the question/pad instead of stacked above it.
- [x] on login screen add remark - 'tafeltikken beta. + version e.g. v0.1 — added "Tafeltikker · beta v0.1" under the avatar grid and under the parent login form
- [x] show invite option in 'ouder' tab, create field to reveal invite url, keep in mind that this wil (later-on) be tafeltikker.oldemans.nl (or other URL configured in env) — added a "+ Ouder uitnodigen" quick action right in the Ouders tab (same generation logic as the full Invites tab); invite URLs are now built server-side using `PUBLIC_BASE_URL` (new env var) when set, falling back to the request's own host — so it'll correctly produce tafeltikker.oldemans.nl links once that's configured, instead of embedding whatever host the admin happened to load the page from
- [x] (user)-admin(s) should be able to add children and assign to parent, user-admin — "+ Kind toevoegen" in the admin Kinderen tab, with a checkbox list of parents to link the new child to (POST /api/admin/children)
- [x] kind-avatar in ouderportal is not working, (can't add due to missing avatar) — real root cause found via live debugging with the user (manual console DOM edits persisted fine; the same insertion done inside the button's click handler consistently ended up empty with no console error): dynamically-built DOM content inserted synchronously *inside a click event handler* was being silently dropped, while pre-existing static fields on the same form (name/PIN inputs) were unaffected. Fixed by (1) building the "add child" avatar picker once up front when the page loads instead of inside the button's click handler, and (2) deferring `buildAvatarPicker`'s DOM work via `setTimeout(fn, 0)` internally so it always runs outside any event handler's call stack — covers the on-demand edit-child form too. Also replaced the native `<select>` with plain clickable emoji buttons (same pattern the kid app's own avatar grid uses). Verified end-to-end with an automated browser test covering both the add and edit flows.
- [x] add status & response to feedback (accepted, need info, planned, fixed, declined) — added `new` as the initial state ahead of those five; admin Feedback tab gets a status dropdown + optional response text per item (PATCH /api/admin/feedback/:id), and the parent's own feedback list shows the current status badge plus the response if one was left
- [x] change default user 'ouder' to admin — SEED_PARENT_USERNAME default is now `admin` (was `ouder`); only affects fresh seeds, doesn't rename any already-existing account
- [x] (user)-admin cannot move kind between 'ouder' — PATCH /api/admin/children/:id now accepts `parentIds`, replacing the child's parent_child links (rejects an empty array so a child can't end up with zero parents); admin child editor's checkbox list is pre-checked with the child's current parent(s) and syncs on save
- [x] from user-admin 'beheer' cannot add feedback in feedback list (just from ouder-view) — admin Feedback tab now has its own submission form (posts to the same /api/parent/feedback endpoint an admin account already owns as a parent row, so it shows up correctly attributed in the combined view too)
- [x] cannot see as what 'username' I'm logged in — both parent.html and admin.html headers now show "Ingelogd als {username}"
- [x] auto continue still seems long with 3s, 1s? — reduced from 3000ms to 1000ms (math + typing)
- [x] when going 'back' mid game, ask for confirmation "Wil je nu al stoppen, weet je het zeker?" — both math and typing's exercise "back" button now show a confirm dialog with that exact text before ending the session; cancelling leaves the exercise running untouched
- [x] the dashboard now shows the "kind toevoegen" button but also the ui to add the kid, that part of the ui should only apear when "kind toevoegen" is clicked — real bug, not just a suggestion: `.child-form`'s CSS set `display: flex` with no `[hidden]` override, and an author stylesheet rule always beats the browser's built-in `[hidden] { display: none }` regardless of order, so the add-child form was visible on page load even though its `hidden` attribute was correctly set and toggled in JS. Same root cause the parent-view/admin-tab toggles already had a guard for, just missed here. Added `.child-form[hidden] { display: none; }`, fixing it for both the parent dashboard's and admin's add-child forms (they share the class). Verified with Playwright that the form is hidden on load and only appears after clicking the button, on both pages.
- [x] I was testing on my ipad(mini) and noticed the layout was still not ideal — three real issues found via a Playwright viewport dump at 1133×744 (iPad mini landscape) plus a screenshot from the user's own device: (1) the existing `≥900px` two-column breakpoint always puts the hint/checklist column on the left and the exercise/numpad on the right regardless of orientation — per the user's explicit direction, added an `orientation: landscape` variant of that breakpoint that swaps the flex `order` so the exercise sits on the side closer to where a hand naturally rests when a tablet/laptop is held in landscape; (2) `#app`'s `max-width: 800px` cap (sized for the narrower portrait/desktop two-column case) left ~330px of dead horizontal margin on a 1133px-wide landscape viewport — bumped to `1100px` for the same landscape breakpoint; (3) the exercise screen's total content height (789px) exceeded the 744px landscape viewport, which combined with `body`'s `align-items: safe center` was clipping ~15-30px off the top of the layout with no way to scroll to it — traced to two elements' unstyled browser-default margins never having been overridden: `.answer-display` (a `<p>`, ~35px top/bottom) and the math question `<h1>` (~30px top/bottom, from the shared `h1` rule sized off this app's 20px root font-size). Tightened `.answer-display`'s margin globally to `12px auto` (in line with every other spacing value in this file) and gave `#math-question` smaller margins specifically within the same landscape breakpoint, which brought total content height under the viewport with room to spare. Verified via the same viewport dump (`#app`'s top position moved from `-61px`/`-15px` to a fully-in-view `21px`) and Playwright screenshots at iPad-landscape, standard desktop-landscape, iPad-portrait, and mobile-portrait to confirm no regressions at any of the other breakpoints; full `npm run test:e2e` suite (15 tests) still passes.

- [x] anoyin bug on tablet, when typing to fast, the display zoom's instead of type — classic double-tap-to-zoom gesture: two fast taps on the same button (e.g. mashing the numpad) were interpreted by the browser as a double-tap-zoom gesture instead of two clicks. Added a single global `button { touch-action: manipulation }` rule covering every button in the app, which tells the browser to skip that gesture on buttons and treat taps as plain clicks only.

## parent dashboard
- [x] progress per mode % correct per challange (letters, woorden, zinnen, sommen per tafel reeks) — Sommen (per-table) and Typen (per-level: letters/woorden/zinnen) both shown now
- [x] developer feedback system — parent dashboard has a feedback textarea + list, backed by a `feedback` table scoped per parent
- [x] bring the same per-item confidence breakdown built for "Sommen" to "Typen" (per-word/letter/sentence accuracy+speed, collapsible) — same "meer ↓"/"minder ↑" pattern as the math fact bars, one row per typing level (Letters/Woorden/Zinnetjes) with a bar per individual prompt from that level's full pool (26 letters/20 words/10 sentences), untried prompts shown as empty placeholders. Confidence formula mirrors `factConfidence` but WPM-based instead of elapsed-ms-based (20 wpm treated as a confident pace for a young typist). Since prompt counts and label lengths vary a lot more than the fixed 10-wide math grid, this row wraps onto multiple lines instead of squeezing into one. Verified with Playwright: played 5 "Letters" prompts as one child and confirmed the dashboard shows exactly 5 non-empty bars out of 26 with the right letters lit up; played "Woorden" with a deliberate typo+correction on 3 of 5 words as the other child and confirmed the stored accuracy (80%/83.3%) is reflected instead of 100%.
- [x] parent-dashboard: add tracking of last runs with status [afgerond[score], afgebroken[number of completed/test-size]] — `practice_sessions` gained `status` (completed/aborted), `target_count`, `completed_count` and `score` columns; both math and typing now report which of these applies when they call `/finish` — a normal finish sends `completed` + the same weighted score already shown on the summary screen (math) or average accuracy (typing), while hitting the exercise "back" button sends `aborted` + however many were done so far. Sessions the browser never got to finish (tab closed mid-exercise) have no status and are left out of the list rather than shown as misleadingly "completed". New "Recente sessies" section on the dashboard lists the last 10 runs (either module) newest-first as e.g. "📐 Sommen ... Afgerond: 87%" or "📐 Sommen ... Afgebroken: 2/10". Verified with Playwright: completed one math run and aborted another, confirmed both show up with the right module, outcome and numbers, newest first.
- [x] remove Sam/Robin, the local demo/test children that ended up in the production database once the beta went live — the app had no way to delete a child at all yet, only create/edit. Added `deleteChild()` (`childUpdates.ts`): deletes a child's `challenge_tables`/`challenges`/`math_attempts`/`typing_attempts`/`practice_sessions`/`parent_child` rows in dependency order inside one transaction, then the child itself — required, since `PRAGMA foreign_keys = ON` is set and none of the FKs cascade on delete. `DELETE /api/parent/children/:id` (ownership-checked, same as the existing PATCH) and a "Verwijderen" button in the child edit form, behind a native confirm() given it's permanent and irreversible. Added `e2e/delete-child.spec.ts` (creates a throwaway child, gives it real practice history, deletes it, confirms both the child and its history rows are gone).
  - Deploying this to production went `git pull` → `npm ci` → `npm run build` → `npm run db:migrate`, but the running Node process was never actually restarted — a real deploy-process gap (not a code bug), so production kept serving the old build with no `DELETE` route at all. The user hit exactly that: "remove does not work — Er ging iets mis bij het verwijderen." Fixed by restarting the service; from then on every redeploy explicitly ends in `systemctl restart tafeltikker`. Verified against the real production instance (over the public HTTPS domain — the internal LAN address alone won't round-trip the login cookie once `COOKIE_SECURE=true` is set, since curl/browsers correctly refuse to send a `Secure` cookie over plain HTTP) that Sam and Robin are now actually gone, with Tim's real data (61 math attempts) untouched.
- [x] on dashboard all excersises show 100% while confidence is low (see screenshot) — real production data (a table answered correctly but slowly) made a genuine implementation gap from way back visible: the per-table rollup line was always plain accuracy (`Tafel 3: 5/5 (100%)`), never the confidence score (accuracy × speed) the per-fact bars underneath it show — the *original* per-fact-confidence request had explicitly asked for the rollup to reflect confidence when collapsed, but the first implementation kept it as accuracy instead (documented as a deliberate-looking choice in `parent-dashboard-metrics.md` at the time, which was itself wrong). The result: a table where every answer was correct but slow could read "100%" while every bar underneath, once expanded, was orange or red — exactly what got reported. Fixed by computing the rollup as the same average-of-per-fact-confidence used for the bars (untried facts still counting as 0), now shown as e.g. "Tafel 3: 62% zelfvertrouwen (24/27 goed)" — confidence as the headline, raw accuracy kept alongside for transparency. Added `e2e/dashboard-confidence-rollup.spec.ts` (answers a table's questions correctly but with a real, deliberate multi-second delay each time, then asserts the rollup shows well under 100% despite perfect accuracy) and corrected `parent-dashboard-metrics.md`'s description of the original (wrong) behavior.

## typing
- [x] add warming up mode — see the more specific follow-up below; both refer to the same implementation
- [x] add typing speed (correct hits) (and log per session to parent dashboard) — WPM/accuracy computed per attempt and now shown per level in the parent dashboard
- [x] add onscreen keyboard (QWERTY) when it takes long, start slowly highlighting the required letter — tappable QWERTY keyboard added (works as a real input method, not just a display); after 3s of no keystroke the next required key highlights, resets on every keystroke
- [x] when focus of the input field is lost, cannot type = confusing — the input now refocuses itself automatically if it loses focus mid-exercise, and tapping the prompt text also refocuses it
- [x] when typo on last letter one cannot backspace, so when correct, continue, when incorrect wait for return — fixed: it only auto-continues on an exact match now; a typo (anywhere, including the last character) stays editable (Backspace works) until Enter is pressed
- [x] on other training tools they start with a 'warming-up', only one row of the keyboard for a few excersise, than two, than full — built into the existing "Letters" level rather than a separate mode: first third of exercises draw only from the home row (a s d f g h j k l), middle third adds the top row, final third is the full alphabet (Woorden/Zinnetjes unchanged)
- [x] when typing errors the success rate stays at 100% even when typos are made and corrected — root cause: accuracy was computed by comparing only the final submitted string, and a corrected typo submits an exact match by definition. Now tracks every character actually typed (correct vs. not, in real time, working for both physical typing and the on-screen keyboard) and computes accuracy from that keystroke count instead; verified with Playwright that a deliberate typo + backspace + correction on "appel" reports 83.3% (5/6 keystrokes correct) instead of 100%
- [x] on dashboard show qwerty layout iso list abcd... parent can observe what area is problematic — the "Letters" level's per-prompt breakdown (see the per-item confidence entry above) now lays out its bars as three physical keyboard rows (qwertyuiop / asdfghjkl / zxcvbnm) instead of one alphabetical a-z line, so a shaky area of the keyboard (e.g. the whole top row) is visually obvious rather than scattered across a sorted list. Exported the on-screen keyboard's existing row grouping from `typing-content.ts` as `QWERTY_ROWS` so both the practice keyboard and the dashboard share one definition. Verified with Playwright that the dashboard renders exactly 3 rows in qwertyuiop/asdfghjkl/zxcvbnm order.
- [x] tested "Woorden" instead of "Letters" and the dashboard still showed a per-word list instead of the QWERTY layout — the QWERTY view above was only ever wired up for the "Letters" level; "Woorden"/"Zinnetjes" still grouped by the literal prompt text, which has too little statistical value once the word/sentence pool grows (each specific word only gets attempted a handful of times, and a much bigger pool would make most bars near-empty). Backend now explodes every attempt's prompt into its individual letters and attributes that attempt's accuracy/wpm to each one (deduped per prompt, so a repeated letter within one word doesn't get double-counted for that attempt) — for "Letters" this is unchanged, since its prompt already is a single letter; "Woorden"/"Zinnetjes" now aggregate the same way, so every level renders through the same QWERTY-row view. Removed the now-dead per-prompt-pool rendering path and the `PROMPT_POOLS` export it was the only user of. Added `e2e/typing-dashboard.spec.ts` covering this for "Woorden".
- [x] the "zinnen" (Zinnetjes) mode's sentences all ended in a "." that had to be typed to complete the exercise, not relevant for this mode — removed the trailing period from every sentence in `typing-content.ts`; typing the words alone now completes it. Verified with Playwright that a sentence typed without any trailing punctuation now submits and scores correctly (also had to correct the test itself along the way — reading the prompt back via the rendered DOM's `textContent` turns the spaces the client renders as `&nbsp;` into actual non-breaking-space characters, which don't match a real keyboard's regular spaces; not an app bug, but worth remembering next time a typing test reads the prompt off the page instead of tracking it directly).
- [x] added a bunch more words/sentences tagged "AVI M4–E4" and asked for a way for the parent to select the word/sentence difficulty per child, with more AVI tiers to follow later — restructured `typing-content.ts`'s Woorden/Zinnetjes content into a `WORD_LEVELS` list (id, label, words, sentences), one entry per AVI tier; only `avi_m4_e4` exists today, holding both the new words and sentences, ready for a second tier (e.g. "AVI E5–M6") to be added as its own entry later. Added `children.avi_level` (defaults to the one existing tier, so nothing changes for the two seeded kids), settable by the parent in the child edit form the same way avatar/name/pin already are — this is deliberately a parent-assigned setting, not something the child picks each session, since it reflects actual reading ability. "Letters" is untouched (not AVI-graded). Verified end-to-end with Playwright and a new `e2e/avi-level.spec.ts`.

## calc
- [x] add keyboard entry + enter to confirm — digits/Backspace/Enter now work on Gemiddeld/Moeilijk (not applicable to Makkelijk's multiple choice)
- [x] the hints now give the solution that should not happen until all tries done — fixed, and difficulty-specific hint policy documented in `math-difficulty-levels.md`
- [x] add progress bar for the chosen amount of exercises — visual bar added, now driven by correct-answer count (see below)
- [x] on simplest mode multiple choice, hide keyboard — already true (no numpad/keyboard listener active in Makkelijk)
- [x] next-timer 3sec iso 5 — auto-advance changed from 5s to 3s (math + typing)
- [x] wrong answers don't count as progress (recorded as success % for parent dash) — a wrong final answer (any difficulty) gets requeued to the end of the session instead of just moving on; the progress bar/counter tracks correct answers against the original chosen amount, while every individual attempt (right or wrong) is still logged for the parent dashboard's % stat
- [x] the program marks 'alle sommen in 1x goed' while there have been multiple attempts, since the calcs are requed until all correct, so you cannot finish with 100% correct score. so 1st = 1, 2nd try counts for 0,66, 3rd for 0,33 per answer — implemented exactly as specified: summary now shows an average score using those weights, and only claims "in één keer goed" when every question scored a full 1.0
- [x] when difficult level 3 is choosen, add timer of 8sec per exercise, than mark failed, que for retry — added an 8s per-try countdown (visible above the question) on Moeilijk only; running out marks that try wrong and follows the same retry/requeue rules as a normal wrong answer
- [x] when the 3tries are done and hint is shown, the 3sec is to fast, maybe not auto forward when completion hint is shown, add hint 'klik op volgende' — auto-advance is skipped only when a hint is showing AND the final answer was wrong (Moeilijk's post-hoc explanation, or Gemiddeld ultimately failing); a correct final try still auto-advances even if Gemiddeld had shown its hint beforehand. Shows "👉 Klik op Volgende als je klaar bent" when paused.
- [x] sometimes the hint is more complex than the exercise e.g. 3x2, hint: 3 × 5 = 15, 3 × 3 = 9, Nu jij: 15 − 9 = ?. I would propose e.g. 3+3 — fixed exactly as proposed: multiplier 2/3/4 now gets plain repeated addition (3×2 → "3 + 3 = ?"); 6–9 keep the ×5/×10 anchor since that's still the simpler path there
- [x] lets fix the 'jumpyness' of the keypad when hint is shown, can we keep the hints panel, empty or create a nice visual rolout from the top? when needed, otherwise show as small empty bar (same color as hint field) — the hint panel is now always present (a thin white bar by default) and animates open via a max-height transition instead of an abrupt show/hide, so the numpad below no longer jumps
- [x] I would propose: 2 major blocks: hints + feedback on the left / excersise + keypad on the right — on ≥900px this is now permanent (not conditional on a hint being shown): left column = hint panel + feedback + the table-progress checklist, right column = question/options/answer/keypad. The table checklist means the left column always has real content, so it never looks like dead space. The question itself now lives in the right column too (it was still sitting in a full-width header above both columns, which is what looked unbalanced) — uses CSS `order` so mobile still sees the question before the hint/feedback block, while wide screens show hints-left/exercise-right.
- [x] we could also use the right block to show the 'tafel' as a list and mark what is completed in that session — added a 1-10 checklist per selected table above the answer area, lighting up each multiplier once answered correctly this session (one row per table when multiple are selected)
- [x] noticed duplicate excersises during a session — root cause: questions were sampled with replacement, so the same table x multiplier could land twice by chance. Now shuffles full "laps" through every fact in the selected tables, guaranteeing no duplicates until a lap is exhausted (only then does a fresh lap start)
- [x] hint panel should reserve fixed space (no rollout animation, no visible placeholder box) and the table-progress cells should use the full column width on one line instead of wrapping — hint panel is now a fixed 140px reserved slot, invisible (no card/border) when empty, no animation; table checklist rows now put the label on its own line above the 10 cells, which stretch to fill the full width so they never wrap
- [x] when the answer was incorrect (after all retries), Volgende does not auto-advance (intentional) but clicking it did nothing — root cause: the shared auto-advance helper's internal "already fired" flag only got reset inside its `start()` method, and the hint-shown-and-wrong path deliberately skips calling `start()` to keep auto-advance paused; the flag stayed stuck at `true` from the previous question's auto-fire, so the manual click was silently ignored. Added a `ready()` method that resets the flag without starting the timer, called from that path. Verified with Playwright: fail all 3 tries on Moeilijk, confirm it doesn't auto-advance, then confirm clicking Volgende actually moves to the next question.
- [x] when choosing difficulty level, add to that page what it means (add icons?): makkelijk "multiple choice" [dice]; gemiddeld "3 pogingen, hint bij laatste poging" [questionmark]; moeilijk: "max 8s per som" [timer] — implemented exactly as proposed: the difficulty picker on the math settings screen switched from a 3-across row of plain label buttons to a stacked list, each with an icon (🎲/❓/⏱️) plus a one-line explanation under the label. Verified visually with Playwright.
- [x] while setting up game, now it has selection for 5/10/20 test, lets add a time option (slider?), when no input received for 1min auto-pause, ask if kind is still there? "Ben je er nog?" — math settings gained an "Aantal"/"Tijd" toggle above the count picker; "Tijd" swaps in a 2/5/10 minuten preset picker instead of a literal slider (matches the existing preset-button style used everywhere else, e.g. table/count/difficulty pickers). A time-mode session ignores the fact count entirely and keeps drawing fresh shuffled "laps" (same no-duplicate-within-a-lap logic as before) until the chosen duration elapses, then ends automatically with the same weighted score as a normal finish. Separately, any 60 seconds with zero input (numpad, options, or physical keyboard) now pauses the exercise — freezing the per-try timer (Moeilijk) and the time-mode countdown so idle time isn't held against the child — and shows a full-screen "Ben je er nog?" overlay with a resume button.
  - Found and fixed a real bug while wiring the mode toggle: the user immediately spotted "now shows selector Aantal and tijd while all options are already shown" — both pickers were visible at once regardless of which mode was selected. Root cause: `.count-picker`'s CSS sets `display: grid` unconditionally, and an author stylesheet rule always overrides the browser's built-in `[hidden] { display: none }` regardless of source order — the exact same class of bug already fixed once this session for `.child-form`. A systematic sweep of every CSS class ever toggled via `.hidden` in the client code turned up four more live instances of the identical bug: `.table-picker` (harmless today, never actually toggled, but latent), `.difficulty-picker` (same), `.math-options` and `.pin-pad` (real bug: switching from Makkelijk to Gemiddeld/Moeilijk without a page reload could leave stale multiple-choice buttons visible on top of the numpad), and `.big-button` — the most significant one, since it's the shared class for the "Volgende" button: the button was visibly rendered (though inert-looking) throughout every question from the very start, before an answer was even given, entirely independent of this session's work, simply never noticed before. Added `[hidden] { display: none; }` overrides for all of them. Verified with Playwright (including using its virtual clock to advance a full 2-minute time-mode session and trigger a 65-second idle pause without a real-time wait) that: the mode toggle now correctly shows only one picker at a time, Volgende is genuinely hidden until an answer is given, a time-mode session ends on schedule with the right summary text, and the idle overlay appears/resumes correctly.
- [x] session summary could claim "alle sommen in één keer goed" (all in one go) even after a wrong answer was made and later corrected — root cause: the score weight per fact (1st try=1.0, 2nd=0.66, 3rd+=0.33) was computed from a counter that reset on every new *appearance* of a question, not tracked per fact across the whole session. A wrong answer gets requeued to reappear later; when that reappearance was answered correctly, it registered as a fresh "try 1" with no memory of the earlier miss — most visibly on Makkelijk, where every appearance is a single, non-retriable guess, so a requeued fact could only ever be scored as a perfect try 1 no matter how many times it had been missed before. Fixed by tracking wrong attempts per fact (table × multiplier) across the entire session instead of per appearance, so a later-corrected fact now correctly scores 0.66/0.33 instead of 1.0. Added a regression test (`e2e/math-scoring.spec.ts`) that deliberately misses the first question, corrects it later, and asserts the summary shows a percentage instead of the "in one go" message.

## challenge module
- [x] built v1 per `doc/challenge-module-design.md` — two challenge types (time played, selectable per module; table confidence, selectable table set using the existing rolling per-fact confidence formula), a fixed sticker preset the parent picks at creation, a permanent completion snapshot re-evaluated after every relevant math attempt/session finish (and again on every read, so a missed hook can't leave a challenge stuck), manual parent reset, a persistent progress widget on the child's home/math-settings/typing-settings screens, and a parent-dashboard section to create/view/reset per child. New `challenges` + `challenge_tables` tables. Public visibility and per-group challenges remain deferred as planned.
  - Found while testing (not fixed, needs a product decision — added as a fresh item in `new-functionality.md`): resetting a table-confidence challenge doesn't actually give the child a fresh shot at it, because confidence is an all-time rolling average rather than scoped to after the challenge's `startedAt` the way time-played is — if the underlying attempts already clear the bar, the very next read just re-completes it instantly. Pinned as an explicit e2e test (`e2e/challenges.spec.ts`) documenting this real current behavior rather than silently "fixing" it without a decision.
- [x] the "Reset" button on a challenge row was unreadable (white text on white background) — root cause: the page's generic `button` rule sets `color: white` for the accent-colored default button style, and `.challenge-reset-button`'s override only reset the background back to white, not the text color, so the label was invisible while still fully clickable. Same for the sticker-picker buttons (harmless there since they only contain emoji, which render in their own color regardless of CSS `color`, but fixed for correctness anyway). Verified visually with Playwright.
- [x] would be nice if the kid hovers over or clicks the challenge card to see the requirement and progress — the widget chip's `title` attribute now shows the full requirement + current progress on hover (desktop), and tapping the chip toggles an expanded detail panel baked into the chip's own markup at render time (no DOM built inside the click handler, so it doesn't need the setTimeout-defer trick), showing e.g. "Word 80% zeker van tafels 3, 4 — je zit nu op 62%." plus the reward line, and collapsing again on a second tap. Added `e2e/challenges.spec.ts` coverage for the tap-to-expand/collapse behavior and its content.
- [x] the "tijd gespeeld" challenge form's Sommen/Typen checkboxes weren't aligned with their labels (checkbox floating above the text instead of next to it) — root cause: the page's generic `label { flex-direction: column }` rule is meant for a text label sitting above its own input, but applied equally to a checkbox's wrapping label, stacking the box above its text instead of beside it. Admin's parent-assignment checkboxes already had a local override for this same issue; added the equivalent as a reusable `.checkbox-label` class in parent.css instead of a one-off fix, so the next checkbox label anywhere in the app gets it for free.
- [x] "error when correct answer" — a correct math answer was reported as wrong, with "Het antwoord is undefined" as the supposed correct answer. Root cause was mine, not the app's game logic: the dev server had been restarted many times this session with no persistent `CHILD_SESSION_KEY`/`PARENT_SESSION_KEY` set, so each restart silently invalidated any already-open browser session (the user's real test tab included) with a fresh throwaway key. The next answer submitted then hit a 401, and the client never checked whether the request actually succeeded before reading `result.correct` off the response — an auth failure and a wrong answer looked identical. Fixed two ways: (1) generated real persistent keys into a local `.env` (already supported by `.env.example`, just never actually loaded — added `dotenv` and `import "dotenv/config"` to the server and db scripts so it's picked up automatically), so restarts during dev no longer log out an open session; (2) hardened the actual bug regardless of cause — math and typing's session-start/attempt-submit fetches now go through a shared `guardedFetch` helper that catches a 401, shows a clear "Je bent uitgelogd" message, and sends the child back to login, instead of silently misreporting a correct answer as wrong. Added `e2e/session-expiry.spec.ts`, which clears cookies mid-exercise and asserts the clear message appears instead of bogus feedback.
- [x] add a difficulty requirement to the tafel-confidence challenge, where a higher tier executed satisfies a lower requirement — math attempts didn't record which difficulty they were answered at, so this needed a new `math_attempts.difficulty` column captured on every attempt, plus a `challenges.required_difficulty` column (nullable, missing/null treated as "easy" — the lowest tier, so existing challenges keep behaving exactly as before). Confidence is now computed only from attempts at or above the required tier (easy < medium < hard), with an unknown/pre-feature difficulty also treated as the lowest tier rather than assumed to satisfy a higher bar. Parent's challenge-creation form gained a "Vereiste moeilijkheidsgraad" select (defaults to Makkelijk — i.e. any tier counts, matching the backward-compatible default); the challenge list and the child-side widget's description both now state it, e.g. "Tafel 3 (Gemiddeld of hoger)". While here, also surfaced the difficulty on "Recente sessies" in the parent dashboard, e.g. "📐 Sommen (Gemiddeld)", derived from any one attempt in that session (a session is always played at one fixed difficulty throughout). Verified with Playwright: perfect Makkelijk practice on a table left a Gemiddeld-or-higher challenge at 0%, and the same table played correctly at Gemiddeld completed it.
- [x] add a 'start' button to the unfolded challenge chip for the child dashboard, that queues the required settings (tafel + difficulty) — for table-confidence challenges only (time-played doesn't pin down a module/table to queue), the expanded chip now has a "Start deze uitdaging →" button. Added `queueMathSettings(tables, difficulty)` to `math.ts`, which pre-selects the given tables and difficulty in the (otherwise persistent, page-lifetime) settings state before opening the math settings screen — the child lands there with the challenge's exact requirements already picked and just taps Start. Hidden once a challenge is already completed, since there's nothing left to queue up for it. Verified with Playwright that tapping it lands on math settings with the right table and difficulty pre-selected.

## testing
- [x] set up a real e2e regression suite — until now every browser-driven test this session ran from an ephemeral scratch directory outside the repo and was thrown away afterward, so nothing accumulated. Added `@playwright/test` to the app, a dedicated `playwright.config.ts` (own port 3100, own `data/e2e-test.sqlite`, reset+reseeded fresh on every run, single worker since tests intentionally share and build on that DB's state) and an `e2e/` directory: `helpers.ts` (login/play-session helpers), `db.ts` (direct DB access for the handful of things impractical to drive through real wall-clock time, e.g. a "played for 30 minutes" challenge), and the first real spec, `e2e/challenges.spec.ts`, covering the full challenge module lifecycle. Run via `npm run test:e2e`. More scenarios (login, math/typing sessions, admin/invite flow) can be backfilled the same way over time.
